{
  "name": "kenya-lost-found-backend",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "cd .. && npx firebase emulators:start --only functions",
    "lint": "echo \"No lint configured\"",
    "serve": "cd .. && npx firebase emulators:start --only functions",
    "deploy": "cd .. && npx firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "@stripe/react-stripe-js": "^5.6.0",
    "@stripe/stripe-js": "^8.7.0",
    "axios": "^1.13.4",
    "dotenv": "^16.6.1",
    "firebase": "^12.9.0",
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^7.0.5",
    "stripe": "^20.3.1",
    "sharp": "^0.33.5",
    "tesseract.js": "^5.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}